# SkiaSharp

MicroblocksQolUtils uses SkiaSharp 4.151.1 and its Windows native runtime for
font and Material Symbol rasterization. The packaged binaries are restored from
NuGet during the normal build; the upstream license is preserved in
`LICENSE.txt` and copied beside the mod binaries.
